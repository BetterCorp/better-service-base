# observable-default

`observable-default` is the default observable plugin that writes structured logs to stdout/stderr.

## Capabilities

- Debug/info/warn/error logging
- Trace-aware message formatting
- Lightweight diagnostics for development and baseline production use

## Notes

This is a core plugin maintained in the main `@bsb/base` project.
