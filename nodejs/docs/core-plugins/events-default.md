# events-default

`events-default` is the in-process events plugin for BSB.

## Capabilities

- Fire-and-forget events
- Returnable events (request/response)
- Broadcast events
- Stream relay support

## Notes

This plugin is intended for single-process deployments and local development.
