{
  "name": "bsb-docs",
  "version": "1.0.0",
  "description": "BSB Documentation Site",
  "scripts": {
    "dev": "vite --host=0.0.0.0",
    "build": "vite build && npm run build-types:nodejs",
    "build-types:nodejs": "cd ../nodejs && npm run docs && cd ../docs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
