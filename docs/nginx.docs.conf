server {
  listen 80;
  server_name www.bsbcode.dev;
  return 301 https://bsbcode.dev$request_uri;
}

server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /404.html;
  }

  location = /404.html {
    internal;
  }
}
